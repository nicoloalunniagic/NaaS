$kind: http-request
name: Get Project
method: GET
url: '{{baseUrl}}/projects/{{projectId}}'
order: 2000
pathVariables:
  - key: projectId
    value: '{{projectId}}'
headers:
  - key: Authorization
    value: Bearer {{authToken}}
