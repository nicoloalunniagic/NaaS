$kind: http-request
name: Delete Project
method: DELETE
url: '{{baseUrl}}/projects/{{projectId}}'
order: 5000
pathVariables:
  - key: projectId
    value: '{{projectId}}'
headers:
  - key: Authorization
    value: Bearer {{authToken}}
